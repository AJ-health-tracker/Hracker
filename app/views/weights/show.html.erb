<p id="notice"><%= notice %></p>

<p>
  <strong>Weight entry for <%= @weight.created_at.strftime("%b %d, %Y") %></strong>
  <br/>
  <br/>
  <%= @weight.weight %>
</p>

<%= link_to 'Edit', edit_weight_path(@weight) %> |
<%= link_to 'Back', weights_path %>
<%= link_to 'Dashboard', root_path %>
